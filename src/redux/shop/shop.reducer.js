import ShopData from './shop.utils'

const INITIAL_STATE = {
    ShopData
}

const ShopReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default ShopReducer